# Linux native host package

This directory is intended to be packed and released as:

- `trace-pilot-host-linux-x86_64.tar.gz`

Expected contents:

- `install.sh`
- `native-host`

Install with:

```bash
chmod +x install.sh
EXTENSION_ID=<your-chrome-extension-id> ./install.sh
```
